import startApp from ".";

/**
 * Entrypoint when the running the application without its own frontend built toolchain.
 * Normally, the application would be expected to kick this off itself.
 */
startApp();
